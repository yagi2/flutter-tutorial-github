name: flutter_tutorial_github
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  freezed_annotation: ^1.0.0
  dio: ^4.0.4
  hooks_riverpod: ^1.0.2
  flutter_hooks: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  freezed: ^1.0.2
  build_runner: ^2.1.5
  json_serializable: ^6.1.1
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true